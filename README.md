npm i --legacy-peer-deps para instalar dependencias!

contruir la imagen docker
<!-- docker build -t nextjs-app . -->
`docker build -t serverflower`

para correr la imagen 
docker run -p 3000:3000 nextjs-app

